package Adaptors.JIRA;

public enum TableColumnName {
    projectName,
    projectUrl,
    issueRepositoryUrl,
    issueRepositoryType,
    username,
    issueRepositoryId,
    priorityName,
    name,
    issueId,
    issueAddress,
    description,
    value,
    customFieldId
}
